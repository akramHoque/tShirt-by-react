import React from 'react';

const Uncle = ({houseAce}) => {
      return (
            <div>
                 <h2>Uncle</h2>
                 <p>House:{houseAce}</p> 
            </div>
      );
};

export default Uncle;